#if(${field.view.lookupView})
  #if(${field.view.showType} == "text")

                        {{model.${field.classname}_label}}
  #elseif(${field.view.showType} == "disabled")

						            <a-form-item :style="{ display: 'inline-block', width: 'calc(100% - 45px)' }">
							              <a-input disabled v-model="model.${field.classname}_label" />
						            </a-form-item>
						            <a-form-item :style="{ display: 'inline-block', width: '45px' }">
							              <a-button disabeld icon="search" />
						            </a-form-item>
  #else

						            <a-form-item :style="{ display: 'inline-block', width: 'calc(100% - 45px)' }">
							              <a-input disabled v-model="model.${field.classname}_label" />
						            </a-form-item>
						            <a-form-item :style="{ display: 'inline-block', width: '45px' }">
							              <a-button icon="search" @click="open${field.className}Lookup}" />
						            </a-form-item>
  #end
#else
#if(${field.inputType} == "text")
  #if(${field.javaType} == "int" || ${field.javaType} == "BigDecimal")
    #if(${field.view.showType} == "text")

                        {{model.${field.classname}}}
    #elseif(${field.view.showType} == "disabled")

                        <a-input-number disabled v-model="model.${field.classname}" />
    #else

                        <a-input-number #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" />
    #end
  #elseif(${field.javaType} == "Date")
    #if(${field.view.showType} == "text")

                        {{model.${field.classname}}}
    #elseif(${field.view.showType} == "disabled")

                        <a-date-picker disabled v-model="model.${field.classname}" />
    #else

                        <a-date-picker #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" />
    #end
  #else
    #if(${field.view.showType} == "text")

                        {{model.${field.classname}}}
    #elseif(${field.view.showType} == "disabled")

                        <a-input disabled v-model="model.${field.classname}" />
    #else

                        <a-input #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" />
    #end
  #end
#elseif(${field.inputType} == "textarea")
  #if(${field.view.showType} == "text")

                        {{model.${field.classname}}}
  #elseif(${field.view.showType} == "disabled")

                        <a-input disabled autosize v-model="model.${field.classname}" />
  #else

                        <a-input #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end autosize v-model="model.${field.classname}" />
  #end
#elseif(${field.inputType} == "select")
  #if(${field.view.showType} == "text")

                        {{filterDictText('${field.dictName}',model.${field.classname})}}
  #elseif(${field.view.showType} == "disabled")

                        <a-select disabled #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" :options="${field.dictName}Options">
                        </a-select>
  #else

                        <a-select #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" :options="${field.dictName}Options">
                        </a-select>
  #end
#elseif(${field.inputType} == "checkbox")
  #if(${field.view.showType} == "text")

                        {{filterDictText('${field.dictName}',model.${field.classname})}}
  #elseif(${field.view.showType} == "disabled")

                        <a-checkbox-group disabled #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" :options="${field.dictName}Options">
                        </a-checkbox-group>
  #else

                        <a-checkbox-group #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" :options="${field.dictName}Options">
                        </a-checkbox-group>
  #end
#elseif(${field.inputType} == "radiobox")
  #if(${field.view.showType} == "text")

                        {{filterDictText('${field.dictName}',model.${field.classname})}}
  #elseif(${field.view.showType} == "disabled")

                        <a-radio-group disabled #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" :options="${field.dictName}Options">
                        </a-radio-group>
  #else

                        <a-radio-group #if(${field.view.placeholder})placeholder="${field.view.placeholder}"#end v-model="model.${field.classname}" :options="${field.dictName}Options">
                        </a-radio-group>
  #end
#else
                        {{model.${field.classname}}}
#end
#end